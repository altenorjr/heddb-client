import React from 'react';
import jss from 'react-jss';
// import cx from 'classnames';

const Home = ({ className, classes, sheet, children}) => (
    <h1>Hoje é dia de Blues</h1>
);

export default jss({
    holder: {

    }
})(Home);